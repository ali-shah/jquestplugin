// subtype knowledge
motif stk
select type,supertype
connected by inherits(type>supertype) and uses(supertype>type)
where "inherits.hasType(8) || inherits.hasType(9)" and "uses.hasType(0)"
group by "supertype"